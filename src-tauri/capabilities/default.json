{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the tray popover UI.",
  "windows": ["popover"],
  "permissions": [
    "core:default",
    "notification:default",
    "core:window:allow-set-size",
    "allow-app-commands"
  ]
}
